name: C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies
      run: sudo apt-get update && sudo apt-get install -y g++ make

    - name: Build
      run: |
        make all:
                  g++ -o MasteroftheMind main.cpp

    - name: Run tests
      run: |
        # Replace with your test command, e.g., ./test or make test
        echo "No tests defined"

name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    # Language setup example for Node.js, Python, and Java
    # Uncomment the section matching your main language

    # Node.js example
    # - name: Set up Node.js
    #   uses: actions/setup-node@v4
    #   with:
    #     node-version: '20'

    # - name: Install dependencies
    #   run: npm install

    # - name: Run tests
    #   run: npm test

    # Python example
    # - name: Set up Python
    #   uses: actions/setup-python@v5
    #   with:
    #     python-version: '3.11'

    # - name: Install dependencies
    #   run: |
    #     python -m pip install --upgrade pip
    #     pip install -r requirements.txt

    # - name: Run tests
    #   run: |
    #     pytest

    # Java example
    # - name: Set up Java
    #   uses: actions/setup-java@v4
    #   with:
    #     distribution: 'temurin'
    #     java-version: '21'

    # - name: Build with Gradle/Maven
    #   run: ./gradlew build # or mvn test

    # Add additional steps for linting, formatting, or deployment as needed.
